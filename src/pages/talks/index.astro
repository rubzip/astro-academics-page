---
import BaseListing from "../../layouts/BaseListing.astro";
import { PAGES } from "../../config";

if (PAGES.talks.active === false) {
    return Astro.redirect("/404");
}
---

<BaseListing
    title={PAGES.talks.title}
    subtitle={PAGES.talks.subtitle}
    collection="talks"
/>
